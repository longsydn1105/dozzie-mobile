<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DozzieHotel" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.DozzieHotel" parent="Base.Theme.DozzieHotel" />

    <!-- Style quy định nền trắng cho Popup -->
    <style name="DozziePopupMenu" parent="Widget.MaterialComponents.PopupMenu">
        <item name="android:popupBackground">@color/white</item>
    </style>

    <!-- Theme quy định màu chữ xanh cho nội dung bên trong Popup -->
    <style name="DozziePopupMenuTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColor">@color/dozzie_blue</item>
        <item name="popupMenuStyle">@style/DozziePopupMenu</item>
    </style>
</resources>